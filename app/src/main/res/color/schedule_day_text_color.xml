<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- This is used when the item is checked -->
    <item android:color="@color/scheduleListItemBackground" android:state_checked="true"/>
    <!-- This is the default text color -->
    <item android:color="@color/scheduleListItemFontColor"/>
</selector>